@model int?

@(Html.Kendo().DropDownListFor(m => m)
    .DataTextField("Name")
    .DataValueField("PlayerID")
    .OptionLabel("Select as player")
    .DataSource(ds => ds.Read(r => r.Action("GetPlayers", "Player"))
    ))
