@model int

@(Html.Kendo().DropDownListFor(m => m)
    .DataTextField("Name")
    .DataValueField("PlayerID")
    .OptionLabel("Select a player")
    .Events(e => 
    {
        e.DataBound("onRedemptionPlayeDropDownDatabound");
        e.Change("onRedemptionPlayerDropDownChange");
    })
    .DataSource(ds => ds.Read(r => r.Action("GetBetParticipantPlayers", "Bet").Data("onRedemptionPlayerDropDownData")).ServerFiltering(true)
    ))

